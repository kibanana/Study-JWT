{
"version": "0.2.0",
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${file}",
            "cwd": "${workspaceRoot}",
            "outFiles": ["${workspaceRoot}/out/*.js"],
            "runtimeExecutable": "${workspaceRoot}/node_modules/.bin/babel-node",
        }
    ]
}